<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RickAndMorty" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- IMAGE STYLES -->
    <style name="ShapeAppearanceOverlay.App.RoundedCorners" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <!-- TEXT INPUT STYLES -->
    <style name="CustomInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@color/appBackground</item>
        <item name="boxStrokeColor">@color/input_stroke_selector</item>
        <item name="hintTextColor">@color/white</item>
        <item name="helperTextTextColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:layout_marginHorizontal">8dp</item>
    </style>

    <style name="CustomInputText">
        <item name="android:textColor">@color/textInputColor</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>
</resources>